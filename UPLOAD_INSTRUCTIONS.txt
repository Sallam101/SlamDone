SlamDone V7.14.3 — Cumulative GitHub root overlay

PRIMARY USE: apply this overlay to the existing SlamDone repository that already has the
V7.14.1 Combined Build + 5-Second Hotfix. It also contains the V7.14.2 corrective changes.

IMPORTANT: THIS ZIP HAS NO EXTRA WRAPPER FOLDER.

1. Extract the ZIP.
2. Inside the extracted location you will immediately see lib/, tool_tests/, pubspec.yaml, etc.
3. Drag/upload those items into the ROOT of the existing SlamDone GitHub repository.
4. Keep the folder paths and replace the matching files.
5. Commit once and let the existing GitHub Pages workflow run.

DATA SAFETY
- No Firebase schema change.
- No local SQLite/database schema change.
- No migration-format change.
- No deletion of existing user progress.
- Journal Delete uses the existing soft-delete/tombstone pattern so Firebase sync propagates it.

Version: 7.14.3+243
