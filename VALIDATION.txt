SlamDone V7.14.1 GitHub Build Base-Href Hotfix — Validation

Root cause confirmed from the failed GitHub Actions log:
Flutter web build requires the exact placeholder:
  <base href="$FLUTTER_BASE_HREF">
The V7.14.1 web/index.html did not contain it.

TDD verification:
- Regression test was first run against the broken index and failed as expected.
- Added exactly one Flutter base-href placeholder.
- Regression test then passed.

Compatibility:
- Keeps the V7.14.1 light floating-timer themes.
- Keeps flutter_bootstrap.js.
- Does not touch the Focus Undo timeout, so it can be applied before or after the 5-second Undo hotfix.
