SlamDone V7.14.1 Combined Build + 5-Second Undo Hotfix — validation targets

Required source contracts:
- Habit horizontal Material Scrollbar does NOT receive crossAxisMargin directly.
- Habit horizontal scrollbar is wrapped in a local ScrollbarTheme whose data uses:
    ScrollbarTheme.of(context).copyWith(crossAxisMargin: _dayHeaderHeight)
- Horizontal scrollbar remains ScrollbarOrientation.top.
- Current-day auto-positioning remains enabled.
- Focus Undo lifetime is exactly Duration(seconds: 5).
- Duration(minutes: 5) is absent from focus_screen.dart.
- web/index.html contains <base href="$FLUTTER_BASE_HREF">.
- Existing light floating timer theme bridge remains present in web/index.html.

Repository contract suite is run against a full V7.14.1 source overlay before packaging.
Flutter itself is not installed in the artifact environment, so the actual `flutter build web`
compile is performed by the repository's GitHub Actions runner after upload.
